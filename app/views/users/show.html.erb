<div class="cards">
      <div class="card-horizontal">
        <%= image_tag @user.avatar, class: "card-horizontal img" %>
        <div class="card-horizontal-infos">
          <h2><%= @user.username %></h2>
          <div>
          <%= link_to "Friend", user_friendships_path(@user) , data: {turbo_method: :post}, class: "btn btn-nav" %>
          <%= link_to "Unfriend", current_user.friendships_as_asker, class: 'btn btn-nav', data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
          </div>
        </div>
      </div>
</div>
